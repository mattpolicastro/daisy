---
- name: apt postgres
  apt:
    name: postgresql
    state: present
    update_cache: yes
    cache_valid_time: '{{ apt_cache_time }}'
- name: apt postgres-contrib
  apt:
    name: postgresql-contrib
    state: present
    update_cache: yes
    cache_valid_time: '{{ apt_cache_time }}'
